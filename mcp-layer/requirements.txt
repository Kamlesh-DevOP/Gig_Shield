# GigShield MCP Layer — dependencies
# Install with: pip install -r mcp-layer/requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.0

# MCP SDK (for inspect-mode and future stdio transport)
mcp[cli]>=1.0.0

# Tavily AI Search & Crawl
tavily-python>=0.5.0

# Async HTTP (used by the backend MCP client)
aiohttp>=3.9.1
